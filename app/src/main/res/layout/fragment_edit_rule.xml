<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="fr.piotr.reactions.EditRuleActivity$PlaceholderFragment">

    <TextView
        android:id="@+id/edit_rule_page_title"
        android:layout_width="match_parent"
        android:layout_height="48sp"
        android:gravity="center_vertical"
        android:textAppearance="?android:textAppearanceMedium"
        tools:text="@string/events"/>

    <GridView
        android:id="@+id/edit_rule_grid"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="48sp"
        android:columnWidth="120sp"
        android:numColumns="auto_fit"
        android:verticalSpacing="8sp"
        android:horizontalSpacing="8sp"
        android:stretchMode="columnWidth"
        android:gravity="center"/>

</LinearLayout>
